# Railway Deployment Environment Variables
# Copy these to your Railway project settings

# === REQUIRED ===

# Database (Railway PostgreSQL will auto-provide this)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT Secret for authentication (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here

# Node Environment
NODE_ENV=production

# === AI PROVIDER (Choose one) ===

# Option 1: Gemini (Recommended for production)
AI_PROVIDER=gemini
GEMINI_API_KEY=your-gemini-api-key-here

# Option 2: Groq (Fast inference)
# AI_PROVIDER=groq
# GROQ_API_KEY=your-groq-api-key-here
# GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Option 3: LM Studio (Self-hosted)
# AI_PROVIDER=lmstudio
# LM_STUDIO_URL=http://your-lmstudio-server:1234/v1
# LM_STUDIO_MODEL=llama-3.2-3b-instruct

# Option 4: Ollama (Self-hosted)
# AI_PROVIDER=ollama
# OLLAMA_URL=http://your-ollama-server:11434/v1
# OLLAMA_MODEL=llava:13b

# === OPTIONAL ===

# ElevenLabs for text-to-speech
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# Server Port (Railway auto-provides this)
PORT=3001
